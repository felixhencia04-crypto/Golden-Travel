import { useRegistration } from './useRegistration';

/**
 * Hook useRegistrasi is an alias for useRegistration to comply with the naming requirement.
 * It manages jamaah registration status and related data.
 */
export function useRegistrasi() {
  return useRegistration();
}

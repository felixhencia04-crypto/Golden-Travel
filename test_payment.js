import fetch from 'node-fetch';
const API_URL = 'http://localhost:3000/api';

async function test() {
  // We need to login as admin to get token
  // Let's get an admin user from DB
  console.log("Checking DB for admin...");
}
test();
